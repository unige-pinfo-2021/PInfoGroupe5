language: java
language: node_js
node_js:
 - 12

branches:
  only:
  - testIntegration

env:
  - NODE_VERSION="12.13.1"
  
services:
  - docker

cache:
    directories:
      - .autoconf
      - $HOME/.m2
      - node_modules


jobs:
  include:
    - stage : Build
      script: 
      #- ./travisScripts/buildtestK.sh
      #- cd testIntegration
      #- ./script.sh
      #- cd ..
      #- ./travisScripts/buildPushServices.sh
      #- ./travisScripts/buildAfterSuccess.sh
      - sudo apt-get update
      - sudo apt-get install openssh-server
      - sudo apt-get install sshpass
      - sshpass -p ${passwd} ssh -o StrictHostKeyChecking=no ${server} ls
